//
//  BaseViewController.swift
//  NavigationControllerSandbox
//
//  Created by Jatin Kindra1 on 02/05/24.
//

import UIKit

class BaseViewController: RootVCViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
//      self.navigationController?.navigationBar.isHidden = true
//      self.navigationController?.navigationItem.leftBarButtonItem?.isHidden = true
//      let navbar = self.navigationController?.navigationBar
//      navbar?.layer.zPosition = -1
//      navigationController?.view.contentInset = UIEdgeInsets(top: 50, left: 0, bottom: 0, right: 0)
//      let overlayView = UIView(frame: CGRect(x: 0, y: 0, width: UIScreen.main.bounds.width, height: 100))
//      overlayView.backgroundColor = UIColor.blue // Example background color
//
//      let overlayWindow = OverlayWindow(frame: UIScreen.main.bounds, overlayView: overlayView)
//      overlayWindow.makeKeyAndVisible()
//      
//      overlayView.frame = CGRect(x: 0, y: 0, width: UIScreen.main.bounds.width, height: 100)
//      
//      overlayView.isUserInteractionEnabled = true
      //        title = "Base VC"
//      let navBar = self.navigationController?.navigationBar
//          navBar?.prefersLargeTitles = true
//          navBar?.frame = CGRect(x: 0, y: 0, width: UIScreen.main.bounds.width, height: 200)
        // Do any additional setup after loading the view.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
